language: erlang
elixir: 1.0.4
services:
  - mongodb
notifications:
  recipients:
    - jerp@checkiz.com
sudo: false
otp_release:
  - 17.4
before_script:
  - sleep 15
  - mix local.hex --force
  - mix deps.get --only test
script: "MIX_ENV=test mix do deps.get, test"
